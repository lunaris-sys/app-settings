<script lang="ts">
  /// System Actions settings page.
  ///
  /// Skeleton from Sprint A — fields wired in Sprint B. Configures
  /// `compositor.toml [system_actions]`: per-action command bindings
  /// for hardware Fn-row keys (Volume, Brightness, Media, Power).
  /// Uses the `CommandStringEditor` component built in Sprint A.

  import Placeholder from "$lib/components/settings/Placeholder.svelte";
</script>

<Placeholder
  title="System Actions"
  description="Hardware key actions — Volume, Brightness, Media, Lock, Power. Wired in Sprint B."
/>
