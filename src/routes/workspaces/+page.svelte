<script lang="ts">
  /// Workspaces & Tiling settings page.
  ///
  /// Skeleton from Sprint A — fields wired in Sprint B. Configures
  /// `compositor.toml [workspaces]` + `[layout]` (gaps, smart_gaps,
  /// tiled_headers, window_rules).

  import Placeholder from "$lib/components/settings/Placeholder.svelte";
</script>

<Placeholder
  title="Workspaces & Tiling"
  description="Workspace layout direction, default tiling mode, gaps, and per-app window rules. Wired in Sprint B."
/>
