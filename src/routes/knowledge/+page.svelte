<script lang="ts">
  /// Knowledge Graph settings page.
  ///
  /// Skeleton from Sprint A — fields wired in Sprint C. Per D10
  /// from the sprint plan, this page is **config-only**: watch
  /// directories, retention period, auto-promote threshold, plus
  /// a small stats snapshot. The browser/timeline UI is a separate
  /// Phase-8 app (`app-timeline`), tracked in ROADMAP.

  import Placeholder from "$lib/components/settings/Placeholder.svelte";
</script>

<Placeholder
  title="Knowledge Graph"
  description="Watch directories, retention, auto-promote threshold, and stats. Browser UI lives in app-timeline (Phase 8). Wired in Sprint C."
/>
